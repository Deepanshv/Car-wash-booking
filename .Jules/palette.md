## 2025-06-06 - [Themed Confirmation Dialogs]
**Learning:** Replacing native browser `window.confirm` with themed MUI `Dialog` components significantly improves UI consistency and allows for better feedback mechanisms like loading states.
**Action:** Use the `Dialog` + `CircularProgress` pattern for all destructive asynchronous actions to provide a professional and clear user experience.
