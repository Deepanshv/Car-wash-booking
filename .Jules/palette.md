# Palette's Journal

## 2025-05-14 - Initial Setup
**Learning:** This application uses MUI v7, which has specific patterns for Grid (using `size` prop) and Dialogs. Native browser confirms are still present and need replacement for better UX consistency.
**Action:** Replace `window.confirm` with themed MUI Dialogs and add Tooltips to icon-only buttons.
